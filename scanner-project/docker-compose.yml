version: '3.8'

services:
  scanner:
    build: ./backend
    container_name: security-scanner
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
